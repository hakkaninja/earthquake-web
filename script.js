function nextPage(e) {
    if (e.keyCode === 13) {
        
        location.href = 'summary.html';
        // run api call function 
        // if going back then clear input box (ie refresh page)
    }
}
